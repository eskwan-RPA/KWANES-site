# KWANES-site

My personal portfolio site — plain HTML, CSS, and a little vanilla JavaScript. No frameworks, no build step.

## Development

Site files live in `public/`. Open `public/index.html` in a browser, or serve the folder locally:

```bash
python -m http.server --directory public
```

## Deploy

Push to `main`; GitHub Actions publishes `public/` to GitHub Pages.
